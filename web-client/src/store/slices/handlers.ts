/**
 * Re-exports from the handlers directory.
 * The handler implementations have been split into domain-specific modules under handlers/.
 */
export { createMessageHandlers } from './handlers/index'
